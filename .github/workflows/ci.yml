name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # prod-solve-group: rgs-django-utils via git (geen sibling-checkout in CI)
      - uses: prefix-dev/setup-pixi@v0.8.10
        with:
          environments: prod
          cache: false
      - run: pixi run -e prod python -m pytest tests/ -v
